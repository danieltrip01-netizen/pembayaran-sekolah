{
  "phases": {
    "extensions": {
      "cmds": [
        "install-php-extensions ctype curl dom fileinfo filter gd hash mbstring openssl pcre pdo session tokenizer xml pdo_mysql redis zip exif"
      ]
    }
  }
}